# ============================================================
# NUNMAI-MAIL — Python dependencies
# ============================================================
# Core data handling
pandas>=2.0.0
numpy>=1.24.0

# Machine learning (classical ML ensemble for phishing classification)
scikit-learn>=1.3.0
joblib>=1.3.0          # for saving/loading trained model to disk

# URL parsing (used to pull out domain/subdomain info from links in emails)
tldextract>=5.1.0

# API serving layer (so other NUNMAI modules / a frontend can call this)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# Testing
pytest>=8.0.0